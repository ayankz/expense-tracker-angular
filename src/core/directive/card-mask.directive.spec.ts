import { CardMaskDirective } from './directive/card-mask.directive';

describe('CardMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new CardMaskDirective();
    expect(directive).toBeTruthy();
  });
});
